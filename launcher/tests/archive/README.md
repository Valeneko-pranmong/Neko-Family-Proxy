# Archived S0 tests

These tests target `NEKO-AUTH-S0 / s0-rc1`, latest-login-wins behavior, or the
missing frozen S0 Core artifact. They are retained as historical evidence and
are intentionally outside pytest discovery during Lite migration.

Do not use them as NEKO-AUTH-LITE acceptance tests. Lite acceptance requires
first-active-session-wins, exact Auth-session permit binding, Lite permit
claims, Core Lite implementation, and coordinated cross-component E2E.

The expected frozen S0 artifact path is not recreated or rebuilt by this
repository: `E:\Temp\neko-phase25-core-final-b3c9d085-FROZEN`.
