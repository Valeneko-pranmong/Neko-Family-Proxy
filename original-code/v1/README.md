# Original code — Launcher V1

This directory preserves the first released Python launcher before the V2
rebuild. It is archived for reference and rollback only.

- `NekoLauncher.py` — original monolithic launcher
- `NekoLauncher.spec` — original PyInstaller build definition

New development belongs under `launcher/`. The original code should not be
used as the implementation source for new features.
