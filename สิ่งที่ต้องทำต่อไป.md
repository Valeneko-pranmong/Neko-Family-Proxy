# แผนคืนระบบอีเมลและกู้คืนรหัสผ่าน

สถานะเอกสาร: แผน + ผลการปรับแก้ local รอบล่าสุด
วันที่ตรวจ: 29 กรกฎาคม 2026
โปรเจกต์หลัก: `F:\Github\Neko-Family-Proxy`  
โปรเจกต์ Admin Tool: `F:\Github\Neko-Family-Proxy-admin-tool`  
Supabase production: `miikoutrnxsunbndecqh` (`Neko-Family-Proxy`)

เอกสารนี้ยังไม่ใช่คำสั่งให้แก้ฐานข้อมูลหรือเปิดใช้ SMTP การเปลี่ยนแปลงภายนอกทั้งหมด
ต้องตรวจ state จริงและขออนุมัติก่อนดำเนินการ

ผลรอบ local ล่าสุด: ปรับ Launcher, migration ที่ยังไม่ apply production,
หน้า reset แบบ static, เอกสาร และชุดทดสอบแล้ว การ replay migration ใน local
ยังทำไม่ได้เพราะเครื่องนี้ไม่มี Docker/Postgres local; production ถูกตรวจแบบ
read-only และยังไม่ได้แก้ Auth user, SMTP, URL หรือ deploy Vercel

## งานที่ยังค้างก่อนเปิดใช้งาน production

รายการต่อไปนี้เป็น handoff ที่ยังต้องทำต่อ ไม่ใช่งานที่ทำเสร็จในรอบ local:

- [ ] Vercel: สร้าง Project โดยใช้ `Root Directory = docs`, สร้าง Preview
      Deployment และ deploy Production พร้อมยืนยัน URL ถาวรของ
      `/reset-password/`
- [ ] Supabase Auth: ใส่ Production URL ใน Redirect URLs และตั้งค่า
      `PASSWORD_RESET_REDIRECT_URL` ใน Launcher; ห้ามใช้ Preview URL เป็นค่าถาวร
- [ ] SMTP: เลือก provider/โดเมนผู้ส่ง, ตั้ง Custom SMTP และปรับ Email Template
- [ ] บัญชีเดิม: ขออีเมลจริงจากเจ้าของบัญชี แล้วเปลี่ยน Auth email ผ่าน
      trusted Supabase Auth Admin API พร้อมตรวจ `profiles.recovery_email`
- [ ] Production migration: ตรวจ migration history แล้ว apply
      `20260729002946_restore_recovery_email_auth_flow.sql` หลังได้รับอนุมัติ
- [ ] Live integration: ทดสอบ reset จริง, Login ด้วย username เดิม,
      license/session/coupon และ Admin Tool หลังเปลี่ยนอีเมล

Hosting ที่เลือกสำหรับหน้า reset: **Vercel**

## เป้าหมายระบบ

- ผู้ใช้กรอก `username` ใน Launcher และ Admin Tool ไม่ต้องกรอกอีเมลตอน Login
- Supabase Auth ใช้ Auth email ที่เป็นอีเมลจริงของผู้ใช้ เพื่อให้ password reset ส่งอีเมลได้
- `public.profiles.recovery_email` เก็บค่าเดียวกับอีเมลกู้คืนและใช้เป็นตัวช่วย lookup
- การสมัครสมาชิกต้องรับอีเมลกู้คืนจริง ตรวจรูปแบบ และบันทึกผ่าน Auth metadata/trigger
- การ Login แปลง `username` เป็น Auth email ผ่าน RPC ที่ควบคุมสิทธิ์ แล้วจึงเรียก
  `signInWithPassword`
- Password reset รับ `username` แล้วส่งลิงก์ไปยังอีเมลกู้คืน โดยตอบข้อความทั่วไป
  เพื่อไม่เปิดเผยว่าชื่อผู้ใช้นั้นมีอยู่หรือไม่
- การยืนยันอีเมลตอนสมัครสมาชิกให้คงปิดไว้ก่อน เพื่อไม่ทำให้ผู้ใช้เดิม Login ไม่ได้
  การยืนยันอีเมลเป็นคนละเรื่องกับการส่งลิงก์กู้คืนรหัสผ่าน

## สถานะปัจจุบันที่ต้องระวัง

- บัญชีเดิมใน production ยังใช้ Auth email ภายใน `@neko-proxy.local`
  ซึ่งไม่ใช่อีเมลสำหรับรับข้อความจริง
- Launcher เวอร์ชันปัจจุบันสร้าง Auth email จากโดเมน Supabase project
  ทำให้บัญชีเดิม Login ไม่ตรง identifier
- Production ยังมี `profiles.recovery_email` และ
  `launcher.auth_email_for_username(text)` อยู่ แต่ migration ใน checkout ปัจจุบัน
  มีไฟล์ที่พยายามลบ RPC นี้ จึงต้องตรวจ migration history ก่อน apply ทุกครั้ง
- ห้ามแก้ `auth.users.email` ด้วย SQL ตรง ๆ และห้ามลบ/สร้างผู้ใช้ใหม่เพื่อแก้ปัญหา
  จนกว่าจะมีแผนย้ายบัญชีและทดสอบ rollback

## งานในโปรเจกต์ Launcher

โฟลเดอร์: `F:\Github\Neko-Family-Proxy`

- [x] คืน field อีเมลกู้คืนในหน้า Register และส่งเป็น argument ของ service
- [x] เพิ่ม validation อีเมลและข้อความแจ้งเตือนที่ไม่เปิดเผยข้อมูลบัญชี
- [x] แก้ `SupabaseGateway.sign_up` ให้ส่งอีเมลจริงไป Supabase Auth
      และส่ง `recovery_email` ใน metadata
- [x] แก้ `SupabaseGateway.sign_in` ให้ใช้
      `launcher.auth_email_for_username` เป็นแหล่ง Auth email
      ไม่สร้างโดเมน synthetic จาก URL แบบปัจจุบัน
- [x] คืน flow “ลืมรหัสผ่าน”: username → lookup recovery email →
      `reset_password_for_email`
- [x] สร้าง/กู้คืน `docs/reset-password/index.html` เป็น static reset page
      สำหรับรับ recovery token และเรียก `updateUser({ password })`
- [ ] สร้าง/เชื่อม Vercel Project สำหรับ repository นี้ โดยตั้ง
      `Root Directory = docs`, Framework = Other/Static และไม่ใช้ build command
- [ ] ให้หน้า reset ใช้เฉพาะ Supabase URL และ Publishable/Anon key
      ผ่าน build configuration; ห้ามใส่ `SUPABASE_SECRET_KEY`
- [ ] สร้าง Preview Deployment เพื่อทดสอบ token และ `updateUser`
      ก่อน Production Deployment
- [ ] Deploy Production บน Vercel และบันทึก URL จริง เช่น
      `https://<vercel-project>.vercel.app/reset-password/`
- [ ] ใส่ URL Production นี้ใน Supabase Redirect URLs และใช้ URL เดียวกัน
      ใน Email Template; ห้ามใช้ Preview URL เป็นค่าถาวร
- [x] เก็บ session/token ด้วย secure store ต่อไป และไม่เก็บรหัสผ่านหรือ SMTP secret
- [x] เพิ่ม/คืน unit tests สำหรับสมัครสมาชิก, username lookup, reset สำเร็จ,
      ไม่มีอีเมล, RPC ล้มเหลว และป้องกัน user enumeration
- [ ] เพิ่ม integration test ด้วยบัญชีทดสอบและอีเมลทดสอบที่ควบคุมได้
- [ ] Build EXE ใหม่และทดสอบ Login, logout, restore session, redeem coupon
      หลังการเปลี่ยนอีเมล
- [x] อัปเดต `supabase/README.md`, `docs/SMTP_SETUP.md` และคู่มือ Launcher

## งานใน Supabase production

งานกลุ่มนี้ต้องขออนุมัติก่อน เพราะอาจแก้ Auth user data และการส่งอีเมลจริง

- [ ] ตรวจ schema/migration history ของ production เทียบกับ checkout ปัจจุบัน
- [ ] ยืนยันว่า `profiles.recovery_email` มี constraint และ unique index ที่ถูกต้อง
- [ ] ยืนยัน/ปรับ `handle_new_user` ให้บันทึก username และ recovery email
      โดยไม่เปิดช่องให้ผู้ใช้แก้ role เป็น admin
- [ ] ยืนยัน RPC สำหรับ username lookup, สิทธิ์ `anon/authenticated`,
      search path และการตอบ error แบบไม่เปิดเผยบัญชี
- [ ] เลือก SMTP provider และโดเมนผู้ส่ง
- [ ] ตั้ง Custom SMTP ใน Supabase Dashboard โดยเก็บ credential นอก repository
- [ ] ตั้ง Site URL และ Redirect URL ให้ตรงกับหน้า reset-password บน Vercel
- [ ] ปรับ Email Template สำหรับ reset password และกำหนดอายุ token
- [ ] ขออีเมลจริงจากเจ้าของบัญชีเดิมก่อนย้ายบัญชี
- [ ] ย้าย Auth email ของบัญชีเดิมผ่าน Supabase Auth Admin API หรือ trusted server
      แล้วอัปเดต/ตรวจ `profiles.recovery_email` ให้ตรงกัน
- [ ] ทดสอบส่ง reset จริง, ใช้ลิงก์ตั้งรหัสผ่านใหม่, Login ด้วย username เดิม
      และตรวจว่า license/session/coupon ไม่เปลี่ยนแปลง
- [ ] ตรวจ Supabase security advisors และบันทึกผลก่อนเปิดใช้งานจริง

## ผลกระทบต่อโปรเจกต์ Admin Tool ใหม่

โฟลเดอร์: `F:\Github\Neko-Family-Proxy-admin-tool`

### ผลกระทบที่คาดว่าจะไม่มี

- `admin-api/src/auth.mjs` ดึง `auth.users` ตาม `profile.id` แล้วใช้
  `authUser.email` แบบ dynamic อยู่แล้ว จึงไม่ผูกกับโดเมน synthetic แบบ Launcher
- การเปลี่ยน Auth email ของผู้ใช้เป็นอีเมลจริงไม่ควรทำให้ Admin Tool ต้องแก้โค้ด
- การเพิ่ม/แก้ `recovery_email` ใน `public.profiles` ไม่กระทบ RPC
  `admin_*`, license, session หรือ coupon
- การตรวจ `profiles.role = 'admin'` และ `profiles.status = 'active'`
  ต้องคงเดิม ไม่ใช้ recovery email เป็นสิทธิ์

### งานที่ต้องตรวจใน Admin Tool

- [ ] ตรวจว่า `.env.local` ชี้ Supabase project เดียวกับ Launcher
      และ `SUPABASE_SECRET_KEY` ไม่ถูกนำไปฝั่ง browser/HTML
- [ ] ทดสอบ Admin Login หลังย้าย Auth email ของบัญชี admin
- [ ] ทดสอบหน้า Users, Licenses, Sessions, Coupons และ Audit
- [ ] ไม่แสดง recovery email เต็มในตารางผู้ใช้โดยค่าเริ่มต้น
      หากต้องแสดงให้ mask และจำกัดเฉพาะ endpoint ฝั่ง server
- [ ] หากเพิ่มคำสั่งแก้ recovery email ในอนาคต ให้ใช้ server-side Admin API,
      audit event และไม่เปิดให้ client เรียก SQL/RPC เปลี่ยนอีเมลเอง
- [x] รัน `npm.cmd test` และ build standalone หลังการเปลี่ยนแปลง

## ตารางผลกระทบข้ามโปรเจกต์

| ส่วน | ต้องแก้หรือไม่ | หมายเหตุ |
|---|---:|---|
| Launcher UI | ต้องแก้ | เพิ่ม/คืนอีเมลตอนสมัครและลืมรหัสผ่าน |
| Launcher Gateway | ต้องแก้ | ใช้ username lookup แทนการเดาโดเมนอีเมล |
| Supabase Auth | ต้องตั้งค่า/ย้ายข้อมูล | SMTP, URL, template และอีเมลของบัญชีเดิม |
| `public.profiles` | ต้องตรวจ; อาจแก้ข้อมูล | ต้องมี recovery email จริงที่ตรงกับ Auth |
| Admin Tool Auth | โดยหลักไม่ต้องแก้ | อ่าน `auth.users.email` แบบ dynamic และตรวจ role/status แยก |
| Admin Tool UI | ไม่ต้องแก้ในรอบแรก | ไม่ควรเปิดเผย recovery email เต็ม |
| Vercel reset page | ต้องเพิ่ม/กู้คืน | เป็น static page แยกจาก Admin Tool และไม่ถือ Secret Key |
| License/Coupon/Session | ไม่ควรมีผล | ใช้ user UUID เดิมและห้ามสร้าง account ใหม่ |

## ลำดับดำเนินการที่ปลอดภัย

1. ตกลง SMTP provider, โดเมนผู้ส่ง และอีเมลจริงของบัญชีเดิม
2. ทำ migration/code ใน branch และรัน unit tests
3. ตรวจ production schema และ snapshot ข้อมูลที่เกี่ยวข้องแบบไม่เปิดเผย secret
4. ตั้ง SMTP/URL/template ใน Dashboard
5. ย้าย Auth email ของบัญชีเดิมผ่าน trusted Auth Admin API
6. ทดสอบ reset และ Login ด้วย Launcher
7. ทดสอบ Admin Tool ด้วยบัญชี admin เดิม
8. ตรวจ license, session, coupon และ audit หลังทดสอบ
9. Build/แพ็ก Launcher และ standalone Admin Tool
10. บันทึกผลและ rollback point ก่อนเปิดให้ผู้ใช้อื่นใช้งาน

## สิ่งที่ยังต้องขอจากเจ้าของระบบ

- SMTP provider ที่ต้องการใช้
- โดเมน/อีเมลผู้ส่งที่ยืนยันแล้ว
- Vercel Project/Team ที่จะใช้ หรืออนุมัติให้สร้าง Project ใหม่
- โดเมนถาวรของหน้า reset หากไม่ใช้ `vercel.app`
- credential สำหรับ SMTP หรือ API key (ส่งผ่านช่องทางลับเท่านั้น
  และห้าม commit ลง Git)
- อีเมลจริงสำหรับย้ายบัญชี `zalovenext`
- อนุมัติให้แก้ Auth user และตั้งค่า Supabase production
