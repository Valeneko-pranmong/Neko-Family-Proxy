# Handoff: ระบบ Login, อีเมลกู้คืน และการเปิด Production

อัปเดตล่าสุด: 29 กรกฎาคม 2026
ผู้รับช่วงต่อ: ทีม Launcher / Supabase / Admin Tool
Repository: `https://github.com/Valeneko-pranmong/Neko-Family-Proxy.git`
Checkout ที่ใช้ตรวจล่าสุด: `D:\Neko-Family-Proxy`
Supabase production: `miikoutrnxsunbndecqh` (`Neko-Family-Proxy`)
Vercel project: `neko-family/neko-family-reset`
Production reset page:
`https://neko-family-reset.vercel.app/reset-password/`

## Handoff update — local continuation

อัปเดตเมื่อ: 29 กรกฎาคม 2026
Branch ปัจจุบัน: `codex/auth-recovery-option-a`
Baseline commit: `6a1fb2ff3cc49852db92a1138711435a682c53ae`

งานที่ทำต่อแล้วใน local branch:

- Launcher ใช้ Auth identifier แบบ deterministic จาก normalized username และ
  Supabase project hostname
- Signup ใช้ synthetic Auth email; recovery email จริงถูกส่งใน metadata
  แยกต่างหาก
- Login และ password reset ไม่เรียก
  `launcher.auth_email_for_username(text)` และไม่ทำ public email lookup
- ปรับ `AuthGateway`, `LauncherService`, unavailable gateway และ unit tests
- เพิ่ม local/dev forward-fix migration:
  `supabase/migrations/20260729120000_secure_option_a_recovery_flow.sql`
- Migration ใหม่นี้แก้ trigger normalization, pin `search_path`,
  ไม่ให้ synthetic Auth email ถูกบันทึกเป็น recovery email และลบ
  `auth_email_for_username(text)` กับ `user_exists(text)`
- อัปเดต README ให้ตรงกับ Option A draft

ผลตรวจสอบล่าสุด:

- `python -m ruff check src tests` ผ่าน
- `python -m pytest -q -m "not integration"`:
  `41 passed, 2 deselected`
- `node --check docs/reset-password/reset.js` ผ่าน
- `node --check docs/reset-password/config.js` ผ่าน
- `git diff --check` ผ่าน

ข้อจำกัดและสิ่งที่ยังห้ามทำ:

- ยังไม่มี approval เลือก Option A อย่างเป็นทางการ
- ยังไม่มี email provider, sender domain/address, provider credential หรือ
  test inbox
- ยังไม่ได้ apply migration ใด ๆ กับ Supabase production
- ยังไม่ได้ deploy Edge Function/Send Email Hook
- ยังไม่ได้เพิ่ม Redirect URL ใน Supabase Auth
- `PASSWORD_RESET_REDIRECT_URL` ยังต้องว่างจนกว่า URL ถาวรจะถูก allow-list
- ยังไม่ได้ทดสอบ recovery E2E ด้วยอีเมลจริง และยังไม่ได้ build EXE release
- เครื่องนี้ไม่มี Supabase CLI จึงยังไม่ได้รัน migration บน local database
- ห้าม apply หรือ replay
  `20260729002946_restore_recovery_email_auth_flow.sql`

งานถัดไปสำหรับผู้รับช่วง:

1. ขอ approval ของ Option A และข้อมูล provider/sender/test inbox ให้ครบ
2. ตรวจ migration history production และ review migration ใหม่บน disposable
   local/dev database
3. ทดสอบ ACL/RLS/trigger และ recovery flow ด้วย test account เท่านั้น
4. Deploy/configure Send Email Hook หลังผ่าน signature, redirect และ
   provider tests
5. ตั้ง Production Redirect URL และ `PASSWORD_RESET_REDIRECT_URL`
6. ทำ canary recovery สำหรับ test account ก่อน existing account
7. ค่อย snapshot และอัปเดต recovery email ของ `zalovenext` ผ่าน trusted
   server/admin path โดยคง UUID/Auth identifier เดิม
8. รัน regression, build release candidate และเตรียม rollback ก่อนเปิดใช้งาน

เอกสารนี้เป็น runbook สำหรับทำงานต่อ ตั้งแต่แก้สถาปัตยกรรม Auth ไปจนถึง
integration test, build และเปิด Production ห้ามข้าม approval gate,
test gate หรือ rollback gate ที่ระบุด้านล่าง

## 1. สรุปสำหรับทีมที่รับช่วง

สิ่งที่ทำเสร็จแล้ว:

- Launcher มี UI สำหรับ username, password และ recovery email
- มีหน้า static สำหรับตั้งรหัสผ่านใหม่ที่
  `docs/reset-password/index.html`
- หน้า reset ใช้เฉพาะ Supabase URL และ Publishable key
  ไม่มี secret/service-role key
- Baseline ก่อน refactor มี unit tests ผ่าน 43 รายการ; local refactor ล่าสุด
  ผ่าน 41 รายการ (ไม่รวม integration) และ Ruff ผ่าน
- JavaScript ของหน้า reset ผ่าน syntax check
- Vercel เชื่อม GitHub แล้ว ตั้ง `Root Directory = docs`,
  Framework = Other และ deploy Production แล้ว
- Vercel Production ตอบ HTTP 200 และมี CSP, HSTS, Referrer Policy,
  `X-Content-Type-Options: nosniff` และ `X-Frame-Options: DENY`
- Supabase production ถูก audit แบบ read-only แล้ว
- `public.profiles.recovery_email` มี format constraint และ unique index
  แบบ case-insensitive
- ทุก public table ที่เกี่ยวข้องเปิด RLS
- ยังไม่มีการแก้ Auth user, Auth URL, Email Hook, SMTP หรือ migration
  production ในรอบล่าสุด

สิ่งที่ยังไม่พร้อม:

- ยังไม่ได้เลือกและอนุมัติสถาปัตยกรรม Auth/Email ขั้นสุดท้าย
- ยังไม่ได้ตั้ง email provider, sender domain หรือ secret
- ยังไม่ได้ deploy/เปิด Send Email Hook
- ยังไม่ได้ใส่ Vercel URL ใน Supabase Redirect URLs
- `PASSWORD_RESET_REDIRECT_URL` ใน Launcher ยังว่าง
- ยังไม่ได้ทดสอบ recovery token และ `updateUser` ด้วยอีเมลจริง
- ยังไม่ได้ build EXE หลังแก้ Auth flow ขั้นสุดท้าย
- ไม่มี checkout ของ Admin Tool ในเครื่องที่ตรวจล่าสุด

## 2. Security blocker ที่ต้องแก้ก่อนทุกอย่าง

ห้าม apply ไฟล์นี้ในสภาพปัจจุบัน:

`supabase/migrations/20260729002946_restore_recovery_email_auth_flow.sql`

เหตุผล:

- Function `launcher.auth_email_for_username(text)` ถูกให้สิทธิ์แก่ `anon`
- Function คืน Auth email จาก username ก่อนตรวจรหัสผ่าน
- ถ้า Auth email เป็นอีเมลจริง จะเปิดเผย PII และใช้ enumerate account ได้
- Function เป็น `SECURITY DEFINER` และข้าม RLS

Production ปัจจุบันยังให้ `anon` และ `authenticated` เรียก:

- `launcher.auth_email_for_username(text)`
- `launcher.user_exists(text)`

ห้ามทำ:

- ห้ามแก้ `auth.users.email` ด้วย SQL ตรง
- ห้ามลบแล้วสร้าง user ใหม่เพื่อแก้ Auth
- ห้ามเปิดเผย secret/service-role key ใน Launcher, HTML หรือ Vercel static files
- ห้ามใช้ `raw_user_meta_data` หรือ user-editable metadata สำหรับ authorization
- ห้าม run/replay migration ทั้งโฟลเดอร์
- ห้ามใช้ Preview URL เป็น Redirect URL ถาวร
- ห้าม log password, recovery token, webhook signature, API key
  หรือ recovery email แบบเต็ม

## 3. Architecture decision ที่เจ้าของระบบต้องอนุมัติ

### Option A — แนะนำ: Auth identifier ภายใน + Send Email Hook

คง Auth email ภายในแบบ deterministic สำหรับ username login และเก็บ
อีเมลรับข้อความจริงไว้เฉพาะ `public.profiles.recovery_email`

Flow ที่ต้องได้:

1. Signup สร้าง Auth identifier ภายในจาก normalized username
2. Signup ส่ง recovery email ใน metadata เพื่อให้ trigger บันทึกลง profile
3. Email confirmation ยังคงปิดในระยะแรก
4. Login derive Auth identifier จาก username โดยไม่ query อีเมลจริง
5. Forgot password derive Auth identifier เดียวกันแล้วเรียก
   `resetPasswordForEmail`
6. Supabase Send Email Hook รับ recovery event
7. Hook verify Standard Webhooks signature
8. Hook lookup `profiles.recovery_email` จาก `user.id` ผ่าน server-side key
9. Hook ส่งลิงก์ recovery ไป recovery email ผ่าน provider เช่น Resend
10. หน้า Vercel รับ token แล้วเรียก `updateUser({ password })`

ข้อดี:

- ไม่เปิดเผยอีเมลจริงให้ public client
- ไม่ต้องเปลี่ยน UUID หรือย้าย Auth user เดิม
- License, session และ coupon ยังผูก user UUID เดิม
- บัญชีเดิมที่ใช้ Auth email ภายในสามารถคงอยู่

ข้อกำหนด:

- ต้องใช้ sender domain ที่ควบคุมได้
- ต้องตั้ง SPF, DKIM และ DMARC
- Hook ต้อง lookup recovery email จากฐานข้อมูลด้วย `user.id`
  ห้ามเชื่อ metadata จาก client เป็นปลายทางโดยตรง
- Hook ต้องรองรับหรือปฏิเสธ email action type อื่นอย่างชัดเจน
  เพราะเมื่อเปิด Hook แล้ว Hook จะรับผิดชอบ Auth email แทน SMTP
- ต้องกำหนด redirect allowlist แบบ exact URL

### Option B — ทางเลือก: Auth email จริง + trusted login proxy

ใช้ Auth email จริงใน `auth.users` แต่ห้ามให้ public client query อีเมลจาก
username ต้องมี trusted backend/Edge Function รับ username+password,
ทำ rate limiting และเรียก Supabase Auth ฝั่ง server

Option นี้มี scope และความเสี่ยงมากกว่า ต้องมี threat model,
abuse protection, monitoring และ penetration test เพิ่ม

### Decision gate

ก่อนเขียน migration หรือ deploy Hook ต้องมีหลักฐานอนุมัติ:

- [ ] เลือก Option A หรือ Option B
- [ ] เลือก email provider
- [ ] ระบุ sender domain และ sender address
- [ ] ส่ง provider credential ผ่าน secret channel
- [ ] มี test inbox ที่ทีมควบคุมได้
- [ ] มี recovery email จริงของบัญชีเดิม `zalovenext`
- [ ] อนุมัติการเปลี่ยน Supabase production

หากไม่มี decision ให้หยุดที่ local design/test ห้ามแก้ production

## 4. สถานะ Production ที่ตรวจพบ

Supabase:

- Project status: `ACTIVE_HEALTHY`
- มี Auth user 1 บัญชี
- Auth email ปัจจุบันยังเป็น `@neko-proxy.local`
- Profile และ Auth identifier ปัจจุบันตรงกัน
- ไม่มี profile ที่ recovery email ว่าง ณ เวลาตรวจ
- `handle_new_user()` ไม่เขียน `role` หรือ `status`
- Client ไม่มีสิทธิ์ update `public.profiles`
- RLS ให้ authenticated อ่าน profile ของตัวเองเท่านั้น
- Migration `remove_recovery_email` และ
  `restore_recovery_email_auth_flow` ไม่อยู่ใน production history
- Timestamp ของ migration production บางรายการไม่ตรงกับชื่อไฟล์ local
  ต้องเทียบด้วย name และตรวจ definition จริง

Supabase Security Advisor:

- Warning: Leaked Password Protection ยังปิด
- Info: `audit_events`, `coupon_redemption_attempts` และ `products`
  เปิด RLS แต่ไม่มี policy โดยตั้งใจ เพราะเป็น server/RPC-only tables
- ต้อง re-run advisors หลัง DDL หรือ Auth config เปลี่ยนทุกครั้ง

Vercel:

- Project ID: `prj_Ob0yANZt7I3zCmT2EhMq0ULopw1g`
- Team: `neko-family`
- Root Directory: `docs`
- Framework: Other
- Preview ที่ตรวจแล้ว:
  `dpl_5kgoesfhhZnfMDBVL7w1nWXS5MyM`
- Production:
  `dpl_EsFdw5gNHgY8kQYjQ5UKSPmyXLec`
- Production URL:
  `https://neko-family-reset.vercel.app/reset-password/`
- Vercel CLI `58.1.0` ถูกติดตั้งและ link repository root แล้ว

Git:

- Baseline commit ที่ deploy หน้า static: `6a1fb2f`
- Working tree หลัง handoff มีการแก้ `.gitignore`
  และเอกสารนี้ที่ยังไม่ได้ commit
- ต้อง review diff แล้ว commit อย่างตั้งใจก่อน push

Admin Tool:

- Path เดิมในเอกสารเก่า:
  `F:\Github\Neko-Family-Proxy-admin-tool`
- ไม่พบโปรเจกต์นี้ในเครื่องที่ตรวจล่าสุด
- ต้อง checkout/ขอ path จากเจ้าของก่อน integration test

## 5. Phase 1 — ทำงานใน branch โดยห้ามแตะ Production

### 5.1 เตรียม branch และ snapshot

- [ ] สร้าง branch ใหม่ เช่น `codex/auth-recovery-production`
- [ ] บันทึก commit SHA เริ่มต้น
- [ ] บันทึก migration history production แบบไม่เปิดเผยข้อมูลผู้ใช้
- [ ] บันทึก function definition และ ACL ของ Auth RPC ปัจจุบัน
- [ ] บันทึก Auth URL/Hook/Provider settings ปัจจุบัน
- [ ] บันทึก Vercel production deployment ID
- [ ] บันทึกจำนวน user/license/session/coupon และ UUID ที่เกี่ยวข้อง
      โดยไม่บันทึกอีเมลเต็มหรือ secret

### 5.2 จัดการ migration ที่เป็น blocker

- [ ] ห้ามปล่อยให้
      `20260729002946_restore_recovery_email_auth_flow.sql`
      ถูก apply อัตโนมัติ
- [ ] ตรวจว่า migration ที่ยังไม่อยู่ใน production ตัวใดควรถอดออกจาก branch
- [ ] สร้าง migration ใหม่ด้วย `supabase migration new <name>`
      หลังเลือก architecture แล้วเท่านั้น
- [ ] Migration ใหม่ต้อง pin `search_path`
- [ ] Revoke `EXECUTE` จาก `PUBLIC` ก่อน grant เฉพาะ role ที่จำเป็น
- [ ] ภายใต้ Option A ให้ถอน public execute จาก
      `auth_email_for_username` และ `user_exists`
- [ ] ยืนยันว่า trigger ไม่รับ `role` หรือ `status` จาก user metadata
- [ ] ยืนยัน constraint/index ของ recovery email
- [ ] Run Security Advisor และ review diff

ห้ามใช้ `supabase db push` จนกว่าจะ reconcile migration history สำเร็จ

### 5.3 แก้ Launcher ตาม Option A

- [ ] กำหนด Auth identifier algorithm เพียงจุดเดียว
- [ ] Signup ใช้ identifier ภายในและส่ง recovery email ใน metadata
- [ ] Login derive identifier จาก username โดยไม่เรียก RPC ที่คืนอีเมล
- [ ] Forgot password derive identifier แล้วเรียก reset
- [ ] UI ตอบข้อความ generic เสมอทั้ง username มีและไม่มี
- [ ] Validation normalize username/email เหมือนกันทุก flow
- [ ] `PASSWORD_RESET_REDIRECT_URL` ยังไม่ใส่จนกว่า Redirect URL ถูก allow-list
- [ ] Session/token ยังเก็บด้วย secure store
- [ ] ห้ามเก็บ password หรือ email provider credential

หากเลือก Option B ให้สร้าง design document และ threat model แยกก่อนเขียนโค้ด

### 5.4 สร้าง Send Email Hook

แนะนำ Supabase Edge Function + Resend หรือ provider ที่อนุมัติ

- [ ] สร้าง function ด้วย Supabase CLI
- [ ] Pin dependency versions
- [ ] รับเฉพาะ method ที่กำหนด
- [ ] Verify Standard Webhooks signature ก่อน parse/use payload
- [ ] ตรวจ timestamp/signature และป้องกัน replay ตาม library ที่ใช้
- [ ] Allowlist `email_action_type`
- [ ] Lookup recovery email ด้วย `user.id` ฝั่ง server
- [ ] ตรวจว่า profile status ใช้งานได้ก่อนส่ง
- [ ] ตรวจ redirect URL แบบ exact match:
      `https://neko-family-reset.vercel.app/reset-password/`
- [ ] สร้าง verify URL จาก token hash ตาม Supabase spec
- [ ] ไม่ log token, email เต็ม, signature หรือ secret
- [ ] Map provider error เป็น response ที่เหมาะสม
- [ ] มี timeout และ retry policy ที่ไม่ส่งอีเมลซ้ำโดยไม่จำเป็น
- [ ] เก็บ provider key, webhook secret และ Supabase server key
      ใน Edge Function secrets เท่านั้น
- [ ] ไม่มี secret อยู่ใน Git, Vercel static config หรือ Launcher

## 6. Phase 2 — Local/Branch testing

### 6.1 Launcher unit tests

รันจาก `launcher`:

```powershell
python -m ruff check src tests
python -m pytest -q -m "not integration"
```

ต้องเพิ่ม/ยืนยัน test ต่อไปนี้:

- [ ] Signup normalize username และ recovery email
- [ ] Signup ใช้ Auth identifier ภายในที่คาดไว้
- [ ] Login username เดิม derive identifier เดิม
- [ ] Login ไม่เรียก RPC ที่เปิดเผย recovery email
- [ ] Forgot password มีและไม่มี username ตอบข้อความเดียวกัน
- [ ] RPC/HTTP/provider error ไม่เผยว่าบัญชีมีอยู่หรือไม่
- [ ] Session restore หลัง restart
- [ ] Logout ล้าง secure session
- [ ] Coupon/session/license flow ไม่เปลี่ยนเพราะ Auth refactor

### 6.2 Edge Function tests

- [ ] Reject request ที่ signature ไม่ถูก
- [ ] Reject request เกิน timestamp window
- [ ] Recovery event ที่ถูกต้อง lookup ด้วย user UUID
- [ ] Missing recovery email ไม่เผย PII
- [ ] Suspended/banned profile ไม่ส่งอีเมล
- [ ] Redirect URL นอก allowlist ถูกปฏิเสธ
- [ ] Provider 4xx/5xx ถูกจัดการและไม่ log secret
- [ ] Duplicate/replayed webhook ไม่สร้างอีเมลซ้ำ
- [ ] Action type อื่นถูก support หรือ reject ตาม design
- [ ] Test output ไม่มี token/email เต็ม

หากมี Docker ให้ใช้ Supabase local + Mailpit
หากไม่มี Docker ให้ใช้ Supabase development branch หลังยืนยันค่าใช้จ่าย
ห้ามใช้ production เป็น environment แรกสำหรับทดสอบ migration

### 6.3 Database/security tests

- [ ] Migration apply บน local/dev branch สำเร็จ
- [ ] Migration rollback/forward-fix ถูกซ้อม
- [ ] `anon` เรียก PII lookup RPC ไม่ได้
- [ ] `authenticated` อ่าน profile คนอื่นไม่ได้
- [ ] Client update `role`, `status`, `recovery_email` โดยตรงไม่ได้
- [ ] Hook/server role อ่านเฉพาะข้อมูลที่ต้องใช้
- [ ] RLS ทุก exposed table ถูกต้อง
- [ ] Function ACL ไม่มี implicit `PUBLIC EXECUTE`
- [ ] Function `search_path` ถูก pin
- [ ] Security Advisor ไม่มี error/warning ใหม่จาก migration

### 6.4 Reset page tests

หน้า Production มีแล้ว แต่ต้องทดสอบกับ token จริงภายหลัง:

- [x] ไม่มี token แสดง “ลิงก์ไม่ถูกต้อง/หมดอายุ”
- [x] DOM/layout/console ผ่าน
- [x] Security headers ผ่าน
- [ ] Recovery token ถูกต้องแสดงฟอร์ม
- [ ] Password สั้นกว่า 8 ตัวถูก reject
- [ ] Password confirm ไม่ตรงถูก reject
- [ ] `updateUser` สำเร็จ
- [ ] URL ถูกล้าง token หลังรับ session
- [ ] Token ใช้ซ้ำไม่ได้
- [ ] Token หมดอายุแสดง generic error
- [ ] หลังสำเร็จ session ถูก sign out

## 7. Phase 3 — Pre-production configuration

ทำหลัง Phase 1–2 ผ่านและได้รับ approval เท่านั้น

### 7.1 Email provider

- [ ] Verify sender domain
- [ ] ตั้ง SPF
- [ ] ตั้ง DKIM
- [ ] ตั้ง DMARC
- [ ] สร้าง sender address เช่น `no-reply@auth.<domain>`
- [ ] แยก Auth email จาก marketing email
- [ ] ตั้ง provider rate limit และ alert
- [ ] เก็บ credential ผ่าน secret manager

หมายเหตุ: โปรเจกต์ Free tier ใหม่ที่ใช้ Supabase default provider
ไม่ควรใช้สำหรับ Production และอาจปรับ template ไม่ได้ ต้องใช้ Custom SMTP
หรือ Send Email Hook

### 7.2 Supabase Auth URLs

ใน Authentication → URL Configuration:

- [ ] ตั้ง Site URL ตาม URL ถาวรที่อนุมัติ
- [ ] เพิ่ม Redirect URL แบบ exact:
      `https://neko-family-reset.vercel.app/reset-password/`
- [ ] ห้ามใช้ Preview URL เป็นค่าถาวร
- [ ] ตรวจว่า redirect อื่นที่มีอยู่ยังจำเป็น
- [ ] บันทึกค่าก่อน/หลังเพื่อ rollback

### 7.3 เปิด Hook

- [ ] Deploy Edge Function version ที่ผ่าน test
- [ ] ตั้ง provider/API secrets
- [ ] สร้างและเก็บ Send Email Hook secret
- [ ] Configure Auth Hook URL
- [ ] เปิด Hook ใน maintenance/canary window
- [ ] ส่ง signed test event ที่ไม่ใช่ production user ก่อน
- [ ] ตรวจ Edge Function logs และ provider delivery logs

### 7.4 Existing account

ภายใต้ Option A:

- [ ] ขอ recovery email จริงของ `zalovenext`
- [ ] Snapshot user UUID, license, session และ coupon state
- [ ] อัปเดตเฉพาะ `profiles.recovery_email`
      ผ่าน trusted server/admin path
- [ ] คง Auth identifier และ user UUID เดิม
- [ ] ตรวจ unique constraint ก่อน update
- [ ] บันทึก audit event โดยไม่ใส่อีเมลเต็ม

ห้ามเปลี่ยน Auth email ของบัญชีเดิมภายใต้ Option A

## 8. Phase 4 — Canary integration test บน Production

ใช้ test account และ test inbox ที่ควบคุมได้ก่อนบัญชีจริง

### 8.1 Test account setup

- [ ] สร้าง test account ผ่าน Launcher flow จริง
- [ ] ยืนยัน username, UUID และ profile
- [ ] ให้ license ทดสอบที่ rollback ได้
- [ ] ใช้ coupon ทดสอบแบบ disposable
- [ ] ห้ามใช้บัญชีเจ้าของระบบเป็น test แรก

### 8.2 Password recovery end-to-end

- [ ] กรอก username ที่ไม่มีอยู่
- [ ] Launcher ตอบ generic success
- [ ] ไม่มีอีเมลถูกส่ง
- [ ] กรอก username test ที่มีอยู่
- [ ] ได้อีเมล 1 ฉบับที่ test inbox
- [ ] From/domain/DKIM/SPF/DMARC ผ่าน
- [ ] Link ใช้ HTTPS และ redirect ไป URL ถาวร
- [ ] Token ไม่ปรากฏใน logs
- [ ] ตั้ง password ใหม่สำเร็จ
- [ ] Password เดิม login ไม่ได้
- [ ] Password ใหม่ login ด้วย username ได้
- [ ] Token เดิมใช้ซ้ำไม่ได้
- [ ] Request ซ้ำ/เร็วเกินถูก rate-limit

### 8.3 Launcher regression

- [ ] Login
- [ ] Logout
- [ ] Restore session หลังปิด/เปิดแอป
- [ ] Claim launcher session
- [ ] Heartbeat session
- [ ] Release session
- [ ] Redeem coupon
- [ ] Coupon replay ถูกปฏิเสธ
- [ ] License expiry/extension ถูกต้อง
- [ ] ProxyCore start/stop
- [ ] Tweaker path และ launch ยังทำงาน

หลัง test ต้องยืนยันว่า user UUID ไม่เปลี่ยนและจำนวน license/session/coupon
ตรงกับ snapshot

### 8.4 Admin Tool regression

เมื่อได้ checkout ของ Admin Tool:

- [ ] `.env.local` ชี้ Supabase project เดียวกัน
- [ ] Secret key อยู่ server-side เท่านั้น
- [ ] Admin login
- [ ] Users
- [ ] Licenses
- [ ] Sessions
- [ ] Coupons
- [ ] Audit
- [ ] ไม่แสดง recovery email เต็มโดย default
- [ ] ทุกคำสั่งแก้ข้อมูลสำคัญมี audit event

## 9. Phase 5 — Build Release Candidate

หลัง integration test ผ่าน:

```powershell
cd D:\Neko-Family-Proxy\launcher
python -m ruff check src tests
python -m pytest -q -m "not integration"
python -m PyInstaller --clean --noconfirm NekoLauncher.spec
```

ก่อน build:

- [ ] ใส่
      `PASSWORD_RESET_REDIRECT_URL =
      "https://neko-family-reset.vercel.app/reset-password/"`
      หลัง Supabase allow-list URL แล้วเท่านั้น
- [ ] ตรวจว่า Publishable key เท่านั้นถูก embed
- [ ] ตรวจว่าไม่มี provider/API/secret key ใน binary
- [ ] ProxyCore directory ที่อนุมัติมีอยู่
- [ ] Version number/release notes ถูกต้อง

หลัง build:

- [ ] บันทึก SHA-256 ของ `dist/NekoLauncher.exe`
- [ ] Scan malware/AV false-positive
- [ ] ทดสอบบนเครื่อง clean
- [ ] ทดสอบ Windows restart/session restore
- [ ] ทดสอบ upgrade จากเวอร์ชันเดิม
- [ ] ทดสอบ rollback เป็น EXE รุ่นก่อน

หากมี Admin Tool standalone ให้ build และบันทึก hash แยก

## 10. Production go-live gate

เปิดให้ผู้ใช้จริงได้ต่อเมื่อทุกข้อผ่าน:

- [ ] Architecture ได้รับอนุมัติ
- [ ] Unsafe email lookup ถูกปิด
- [ ] Migration ผ่าน dev branch และ production review
- [ ] Security Advisor ถูก review
- [ ] Sender domain SPF/DKIM/DMARC ผ่าน
- [ ] Hook signature/redirect allowlist ผ่าน
- [ ] ไม่มี secret/PII/token ใน Git หรือ logs
- [ ] Unknown-user flow ไม่ enumerate account
- [ ] Recovery E2E ผ่านด้วย test account
- [ ] Existing-account canary ผ่าน
- [ ] UUID/license/session/coupon ไม่เปลี่ยน
- [ ] Admin Tool regression ผ่าน
- [ ] EXE release candidate ผ่าน clean-machine test
- [ ] มี rollback owner และ rollback window
- [ ] มี snapshot ก่อน production change
- [ ] มีช่องทางแจ้งเหตุและ support owner

ถ้าข้อใดไม่ผ่าน ให้หยุด rollout

## 11. ลำดับเปิด Production

1. Freeze schema/config change อื่น
2. Snapshot migration/function/ACL/Auth config และข้อมูลเชิงจำนวน
3. Apply migration ที่ review แล้วเพียงรายการเดียว
4. Query ยืนยัน schema, grants, RLS และ function definition
5. Run Security Advisor
6. Deploy Edge Function version ที่ผ่าน test
7. ตั้ง secrets
8. ตั้ง Site URL และ Redirect URL
9. เปิด Send Email Hook
10. Run test-account recovery E2E
11. Update recovery email ของบัญชีเดิมผ่าน trusted path
12. Run existing-account canary
13. Deploy Launcher release candidate ให้กลุ่มเล็ก
14. Monitor อย่างน้อยหนึ่ง canary window
15. ขยาย rollout เมื่อ metrics/logs ปกติ
16. บันทึกผล, deployment IDs, hashes และ rollback point

## 12. Monitoring หลังเปิด

ช่วง 24 ชั่วโมงแรก:

- Supabase Auth logs
- Edge Function errors/latency
- Provider delivery, bounce, complaint และ rate limit
- จำนวน password-reset requests
- จำนวน invalid/expired/reused tokens
- Login failure rate
- Session claim/reject rate
- Coupon redemption/replay failures
- License errors
- Admin audit events
- Vercel availability ของ reset page

ห้าม log username+email เต็มคู่กัน

Rollback ทันทีหาก:

- ส่งอีเมลผิดผู้รับ
- มี token/secret/PII ใน logs
- Existing user login ไม่ได้
- User UUID เปลี่ยนหรือเกิด duplicate user
- License/session/coupon ผิดจาก snapshot
- Hook failure ทำให้ Auth email ล้มเหลวต่อเนื่อง
- Redirect ไป domain/path ที่ไม่อนุมัติ

## 13. Rollback plan

เตรียมก่อน rollout:

- Previous Vercel deployment ID
- Previous Launcher EXE และ SHA-256
- Previous Edge Function version
- Previous Auth URL/Hook settings
- Forward-fix migration สำหรับ ACL/function
- Snapshot user UUID/license/session/coupon

ขั้น rollback:

1. หยุด rollout Launcher
2. Disable Send Email Hook หาก Hook เป็นสาเหตุ
3. Roll back Edge Function version หรือ Auth Hook config
4. Restore Auth URL/config จาก snapshot
5. Apply forward-fix migration ที่ review แล้ว
6. Roll back Vercel alias หากหน้า reset เป็นสาเหตุ
7. แจก Launcher รุ่นก่อน
8. ตรวจ user UUID/license/session/coupon
9. บันทึก incident และ root cause

ห้าม rollback ด้วยการลบ user หรือแก้ `auth.users` ด้วย SQL ตรง

## 14. คำสั่งตรวจที่ใช้บ่อย

Launcher:

```powershell
cd D:\Neko-Family-Proxy\launcher
python -m ruff check src tests
python -m pytest -q -m "not integration"
```

หน้า reset:

```powershell
node --check D:\Neko-Family-Proxy\docs\reset-password\reset.js
node --check D:\Neko-Family-Proxy\docs\reset-password\config.js
curl.exe --head https://neko-family-reset.vercel.app/reset-password/
```

Vercel:

```powershell
vercel.cmd project inspect neko-family-reset --scope neko-family
vercel.cmd inspect https://neko-family-reset.vercel.app --scope neko-family
```

Supabase:

- ใช้ `supabase --help` และ `supabase <command> --help` ก่อนทุกคำสั่ง
- ใช้ Supabase MCP `list_migrations`, `execute_sql` แบบ read-only
  และ `get_advisors` สำหรับ production audit
- ห้าม run `db push` จนกว่า migration history จะ reconcile แล้ว

## 15. ข้อมูลที่ทีมต่อไปต้องขอ

- [ ] Approval ของ Option A หรือ Option B
- [ ] Email provider
- [ ] Sender domain/address
- [ ] Provider credential ผ่าน secret channel
- [ ] Test inbox
- [ ] Recovery email จริงของ `zalovenext`
- [ ] Approval ให้เปลี่ยน Supabase production
- [ ] Path/repository และ credential ของ Admin Tool
- [ ] Maintenance/canary window
- [ ] Rollback owner

## 16. เอกสารอ้างอิง

- Supabase Send Email Hook:
  `https://supabase.com/docs/guides/auth/auth-hooks/send-email-hook`
- Supabase Resend Hook example:
  `https://supabase.com/docs/guides/functions/examples/auth-send-email-hook-react-email-resend`
- Supabase password reset:
  `https://supabase.com/docs/guides/auth/passwords`
- Supabase production checklist:
  `https://supabase.com/docs/guides/deployment/going-into-prod`
- Supabase testing:
  `https://supabase.com/docs/guides/local-development/cli/testing-and-linting`
- Vercel Production:
  `https://neko-family-reset.vercel.app/reset-password/`
