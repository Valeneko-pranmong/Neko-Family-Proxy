# สิ่งที่ต้องทำต่อไป – ระบบลืมรหัสผ่าน

> ตรวจสอบเมื่อ 26 ก.ค. 2569

## สถานะปัจจุบัน

ระบบลืมรหัสผ่านมี flow พื้นฐานครบ (UI → Service → Gateway → Supabase Auth)
แต่ยังมีจุดที่ต้องแก้ไขก่อนใช้งานจริงได้

---

## งานที่ต้องทำ

### 1. Backfill recovery_email สำหรับผู้ใช้เก่า
- ผู้ใช้ที่สมัครก่อน migration `20260726112000` ยังไม่มี `recovery_email` ในตาราง `profiles`
- ต้องเขียน migration เพื่อ populate จากข้อมูลเดิม (เช่น `auth.users.email` ก่อนที่จะถูกเปลี่ยนเป็น synthetic)
- ถ้าไม่มีอีเมลจริงเก็บไว้ ต้องให้ผู้ใช้เก่ากรอกอีเมลเองผ่าน UI

### 2. สร้าง redirect page สำหรับรับ reset token
- Desktop app ไม่สามารถรับ URL redirect จาก Supabase ได้โดยตรง
- ทางเลือก:
  - สร้างหน้าเว็บกลาง (hosted) ที่รับ token จาก Supabase แล้วแสดงฟอร์มตั้งรหัสผ่านใหม่
  - หรือใช้ custom URL scheme (`neko-launcher://reset?token=...`) ที่ OS จะเปิด app
- ต้องตั้งค่า redirect URL ใน Supabase Dashboard ให้ชี้ไปที่หน้านี้

### 3. เพิ่ม UI "ตั้งรหัสผ่านใหม่" ใน app
- ปัจจุบัน flow จบแค่ "ส่งลิงก์แล้ว" → ไม่มีขั้นตอนต่อ
- ต้องมี UI ที่ผู้ใช้กรอกรหัสผ่านใหม่หลังกดลิงก์ในอีเมล
- เชื่อมกับ `LauncherService.change_password()` หรือ `auth.update_user()`

### 4. ตั้งค่า SMTP สำหรับ production
- `supabase/config.toml` ไม่ได้ตั้งค่า SMTP
- ต้องตั้งค่าใน Supabase Dashboard:
  - SMTP host, port, user, password
  - Sender email address
  - Email template สำหรับ password reset (ภาษาไทย)
- ทดสอบว่าอีเมลส่งถึงจริง (ไม่เข้า spam)

### 5. พิจารณาให้กรอก username แทน email ในหน้าลืมรหัสผ่าน
- ปัจจุบัน UI ให้กรอก "อีเมลที่ใช้สมัคร"
- แต่ผู้ใช้อาจจำ username ได้ดีกว่าอีเมล
- ถ้าเปลี่ยนเป็นกรอก username → ระบบ lookup `recovery_email` จาก `profiles` → ส่ง reset ไปยังอีเมลที่ถูกต้อง
- ข้อดี: ทำงานได้กับทั้งผู้ใช้เก่าและใหม่ (ถ้า backfill recovery_email แล้ว)

### 6. เพิ่ม test cases
- ปัจจุบันไม่มี test สำหรับ password reset (stub เป็น no-op)
- ต้องเพิ่ม test ครอบคลุม:
  - อีเมลผิดรูปแบบ → raise error
  - อีเมลที่ไม่มีในระบบ → ไม่ error (เพื่อป้องกัน user enumeration) แต่ต้อง log
  - Gateway error → raise LauncherServiceError
  - Happy path → เรียก `reset_password_for_email` สำเร็จ

### 7. จัดการ silent failure
- Supabase ไม่ return error เมื่ออีเมลไม่มีในระบบ (ป้องกัน user enumeration)
- UI แสดง "ส่งลิงก์แล้ว" ทุกกรณี → ผู้ใช้พิมพ์อีเมลผิดก็ไม่รู้ตัว
- พิจารณาเพิ่มคำแนะนำใน UI เช่น "หากไม่ได้รับอีเมลภายใน 5 นาที กรุณาตรวจสอบว่าอีเมลถูกต้อง"

---

## ไฟล์ที่เกี่ยวข้อง

| ไฟล์ | บทบาท |
|------|--------|
| `launcher/src/neko_launcher/ui/app_window.py` | UI แท็บ "ลืมรหัสผ่าน" |
| `launcher/src/neko_launcher/application/services.py` | `request_password_reset()` – validate + เรียก gateway |
| `launcher/src/neko_launcher/application/ports.py` | `AuthGateway.request_password_reset()` – interface |
| `launcher/src/neko_launcher/infrastructure/supabase_gateway.py` | `reset_password_for_email()` – เรียก Supabase Auth |
| `supabase/config.toml` | ตั้งค่า auth (ยังไม่มี SMTP) |
| `supabase/migrations/20260726112000_add_recovery_email_auth_lookup.sql` | เพิ่ม recovery_email + auth_email_for_username RPC |
| `supabase/migrations/20260726090127_migrate_auth_identifiers_to_username.sql` | เปลี่ยน auth email เป็น synthetic |
| `launcher/tests/test_services.py` | test stub (ยังไม่มี test จริง) |

---

### 8. กู้คืน CI/CD workflows (GitHub Actions)
- `.github/workflows/` ถูกลบใน commit `8f3ae1b` ("v2") ทำให้ไม่มี CI เลย
- Workflows ที่ถูกลบ:
  1. **`ci.yml`** — repository safety check + Ruff lint + pytest (push to main, PR)
  2. **`release.yml`** — build EXE + Installer + GitHub Release (manual / tag `v*`)
  3. **`supabase-integration.yml`** — integration test กับ Supabase จริง (manual)
- เอกสารหลายไฟล์ยังอ้างถึง workflows เหล่านี้ (`REPOSITORY_LAYOUT.md`, `CONTRIBUTING.md`, `supabase/README.md`)
- **ต้องทำ:**
  - กู้คืน `ci.yml` และ `supabase-integration.yml` — พร้อมใช้กับ codebase ปัจจุบัน
  - ปรับแก้ `release.yml` — อ้างอิง `installer/NekoLauncher.iss` ที่ยังไม่มีใน repo

---

## ลำดับความสำคัญ

1. 🔴 **ตั้งค่า SMTP** – ไม่มี SMTP = ส่งอีเมลไม่ได้เลย
2. 🔴 **สร้าง redirect page / deep link** – ไม่มี = ผู้ใช้กดลิงก์แล้วไปไม่ถูกที่
3. 🔴 **กู้คืน CI workflows** – ไม่มี CI = ไม่มีการตรวจ leak secrets และ regression อัตโนมัติ
4. 🟡 **เพิ่ม UI ตั้งรหัสผ่านใหม่** – ต้องทำคู่กับ redirect page
5. 🟡 **Backfill recovery_email** – จำเป็นถ้ามีผู้ใช้เก่า
6. 🟢 **เปลี่ยนเป็นกรอก username** – ปรับปรุง UX
7. 🟢 **เพิ่ม test cases** – ป้องกัน regression
8. 🟢 **จัดการ silent failure** – ปรับปรุง UX
