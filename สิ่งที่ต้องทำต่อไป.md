# สิ่งที่ต้องทำต่อไป – ระบบลืมรหัสผ่าน

> อัปเดตเมื่อ 27 ก.ค. 2569

## สถานะปัจจุบัน

ระบบลืมรหัสผ่านมี flow ครบทุกขั้นตอน:
**UI (กรอก username) → Service → Gateway → Supabase Auth → อีเมล → Redirect Page (ตั้งรหัสใหม่)**

---

## ✅ งานที่เสร็จแล้ว

### ~~5. เปลี่ยนให้กรอก username แทน email ในหน้าลืมรหัสผ่าน~~ ✅
- UI เปลี่ยนจาก "กรอกอีเมล" → "กรอกชื่อผู้ใช้"
- เพิ่ม `lookup_recovery_email()` ใน `AuthGateway` port
- Implement ใน `SupabaseGateway` (เรียก RPC `auth_email_for_username`)
- Implement ใน `UnavailableGateway` (raise error)
- `LauncherService.request_password_reset()` รับ username → lookup email → ส่ง reset
- Silent success เมื่อไม่พบ email หรือ network error (anti-enumeration)

### ~~6. เพิ่ม test cases~~ ✅
- 5 test cases ใหม่ใน `test_services.py`
- รวมทั้งหมด 32 tests ผ่าน

### ~~7. จัดการ silent failure~~ ✅
- Service ไม่ raise error เมื่อ username ไม่มีในระบบ (anti-enumeration)

### ~~8. กู้คืน CI/CD workflows~~ ✅
- `.github/workflows/ci.yml` — repository safety check + Ruff lint + pytest
- `.github/workflows/release.yml` — build EXE + Installer + GitHub Release
- `.github/workflows/supabase-integration.yml` — integration test กับ Supabase

### ~~1. Backfill recovery_email สำหรับผู้ใช้เก่า~~ ✅
- สร้าง migration `20260727200000_backfill_recovery_email.sql`
- Populate `recovery_email` จาก `raw_user_meta_data` หรือ `auth.users.email` (ถ้าไม่ใช่ synthetic)
- ผู้ใช้ที่ไม่มีอีเมลจริงจะยังคง NULL (ต้องกรอกเองในอนาคต)

### ~~2. สร้าง redirect page สำหรับรับ reset token~~ ✅
- สร้าง `docs/reset-password/index.html` — static page ที่รับ token จาก Supabase
- รองรับทั้ง hash fragment (`#access_token=...`) และ query param (`?token_hash=...`)
- ฟอร์มตั้งรหัสผ่านใหม่พร้อม validation
- Deploy ผ่าน GitHub Pages (`/docs` folder)

### ~~3. เพิ่ม UI "ตั้งรหัสผ่านใหม่"~~ ✅
- ใช้ redirect page บนเว็บแทน UI ในแอป (desktop app ไม่สามารถรับ URL redirect)
- ผู้ใช้คลิกลิงก์ในอีเมล → เปิดเว็บ → กรอกรหัสผ่านใหม่ → กลับไปเข้าสู่ระบบในแอป

### ~~4. สร้างคู่มือตั้งค่า SMTP สำหรับ production~~ ✅
- สร้าง `docs/SMTP_SETUP.md` — คู่มือครบทุกขั้นตอน
- เลือก SMTP Provider, ตั้งค่าใน Dashboard, ปรับ Email Template, Deploy redirect page

---

## ⚠️ งานที่ต้องทำด้วยมือ (Manual Setup)

สิ่งเหล่านี้ต้องทำใน Supabase Dashboard หรือ hosting โดยตรง:

### 1. 🔴 ตั้งค่า SMTP ใน Supabase Dashboard — รอ SMTP credentials
- ดูคู่มือ: [`docs/SMTP_SETUP.md`](docs/SMTP_SETUP.md)
- ตั้ง custom SMTP (Resend / Brevo / Mailgun)
- ปรับ Email Template สำหรับ password reset

### ~~2. ตั้ง Redirect URL ใน Supabase Dashboard~~ ✅
- ตั้ง Site URL และ Redirect URL เป็น `https://valeneko-pranmong.github.io/Neko-Family-Proxy/reset-password/`

### ~~3. Deploy redirect page~~ ✅
- เปิด GitHub Pages จาก `main` branch และ `/docs` folder

### ~~4. อัปเดต Anon Key ใน redirect page~~ ✅
- ใช้ publishable key ของ production project ใน `docs/reset-password/index.html`

### ~~5. Apply migration ใน production~~ ✅
- Applied `supabase/migrations/20260727200000_backfill_recovery_email.sql`

---

## ไฟล์ที่เกี่ยวข้อง

| ไฟล์ | บทบาท |
|------|--------|
| `launcher/src/neko_launcher/ui/app_window.py` | UI แท็บ "ลืมรหัสผ่าน" (กรอก username) |
| `launcher/src/neko_launcher/application/services.py` | `request_password_reset()` – validate + lookup email + เรียก gateway |
| `launcher/src/neko_launcher/application/ports.py` | `AuthGateway.lookup_recovery_email()` + `request_password_reset()` |
| `launcher/src/neko_launcher/infrastructure/supabase_gateway.py` | `lookup_recovery_email()` + `reset_password_for_email()` |
| `launcher/src/neko_launcher/infrastructure/unavailable_gateway.py` | Offline fallback |
| `supabase/config.toml` | ตั้งค่า auth (ยังไม่มี SMTP) |
| `supabase/migrations/20260726112000_add_recovery_email_auth_lookup.sql` | เพิ่ม recovery_email + auth_email_for_username RPC |
| `supabase/migrations/20260726090127_migrate_auth_identifiers_to_username.sql` | เปลี่ยน auth email เป็น synthetic |
| `supabase/migrations/20260727200000_backfill_recovery_email.sql` | Backfill recovery_email สำหรับผู้ใช้เก่า |
| `docs/reset-password/index.html` | Redirect page สำหรับตั้งรหัสผ่านใหม่ |
| `docs/SMTP_SETUP.md` | คู่มือตั้งค่า SMTP |
| `launcher/tests/test_services.py` | 32 tests รวม password reset tests |
| `.github/workflows/ci.yml` | CI pipeline (lint + test) |
| `.github/workflows/release.yml` | Release pipeline (build + publish) |
| `.github/workflows/supabase-integration.yml` | Supabase integration tests |
